<div class="container">
    <div class="box">
        <div class="primary">{{{name}}}</div>
        <div class="content">{{{body}}}</div>
        <div class="secondary">Email: {{{email}}}</div>
        <div class="secondary">Comment with id: {{{id}}}</div>
        <div class="secondary">Post with id: {{{id}}}</div>
    </div>
</div>
